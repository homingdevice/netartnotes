terminal: ls = list
          cd = create directory
          clear = clear previous
          CNTRL + C = quit
          sudo (b4 a command prompt will let your run w/e u want just put in ur psswrd)

          ssh = secureshell
